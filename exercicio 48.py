#faça um programa que calcule a soma
#  entre todos os nùmeros impares que são
# mùltiplos de tres e que se encontram
#  no intevalo de 1 até 500.
s1 = 0
s2 = 0
for c in range(1, 501, 2):
  if c % 3 == 0 :
      s1 += + 1
      s2 += c
print('A soma de todos os {} valores solicitados é {}.'.format(s1,s2))

























